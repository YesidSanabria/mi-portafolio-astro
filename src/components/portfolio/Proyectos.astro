---
import Card from '../ui/Card.astro';
import { getCollection } from 'astro:content';
const proyectos = await getCollection('proyectos');
console.log(proyectos);
---
<section id="proyectos" class="hero">
    <div class="flex flex-col items-center text-center">
        <h1 class="mx-auto mt-2 max-w-max text-center font-bold tracking-tight text-balance text-[var(--text-primary)] sm:text-5xl">
            <span class="text-[var(--text-terceary)]">Proyectos</span>
            Terminados
        </h1>
        <p class="z-2 text-center mx-auto text-lg max-xl:text-base max-w-2xl text-[var(--text-primary)] my-6 leading-6 font-medium">
        Me encanta <b class="text-[var(--text-terceary)]">convertir ideas en proyectos reales.</b> <br> Aquí les muestro algunos de los <b class="text-[var(--text-terceary)]">desarrollos</b> en los que he trabajado, aplicando tecnología, diseño y mucha creatividad.
        <b class="text-[var(--text-terceary)]">Hagan un vistazo!</b> </p>
        <Card />
        <a href="/" class="proy">          
          Ver todos los proyectos
        </a>
    </div>
</section>


<style>
.hero {
    margin-top: 100px;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 60vh; 
    overflow-x: hidden;
}
.container {
    margin-top: 40px;
    width: 100%;
    height: 100%;
    gap: 4px;
	align-items: center;
}
.hero h1{
    padding-bottom: 20px;
}
.hero p{
    text-align: center;
}
.proy{

    padding-top: 60px;
}